# Core
numpy>=1.26
pillow>=10.0
opencv-python-headless>=4.9
python-dotenv>=1.0
pydantic>=2.6
httpx>=0.27
rich>=13.7

# Vision matching (Agent 1 / Mapillary)
torch>=2.2
open-clip-torch>=2.24
scikit-image>=0.22

# OCR (Landmark agent)
easyocr>=1.7

# Optional: Cursor SDK for VLM + judge orchestration (phase 2)
cursor-sdk>=0.1.0
