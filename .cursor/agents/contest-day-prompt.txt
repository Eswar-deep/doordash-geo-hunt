DoorDash FIFA drop — run the geo-hunt pipeline.

Tweet URL:
PASTE_URL_HERE

IMPORTANT:
- Do NOT open x.com or try to read the tweet in a browser — X blocks that.
- Do NOT use web search to find the tweet.
- ONLY use our ingest script (FxTwitter API inside the repo).

In repo root:
1. pip install -e .
2. python cli.py ingest "PASTE_URL_HERE" --out samples/live-drop --run --tweet-id

If ingest fails, retry once. If still failing, tell the user to save photo 2 (map) and photo 3 (clue) from the X app and run:
  python cli.py run --map PATH/photo2.jpg --location PATH/photo3.jpg --city CITY --output-json output/live.json

Reply with ONLY:
- Final lat/lng
- Confidence
- Winner agent
- Google Maps link: https://www.google.com/maps?q=LAT,LNG
- Path to output JSON
